# 🤖 assistant-bot

CLI-асистент для керування контактами, адресами, email-ами та нотатками. Бот підтримує CRUD-операції, нагадування про дні народження, кольоровий вивід (`colorama`), табличне форматування (`tabulate`) та автодоповнення команд першим словом (`prompt_toolkit`).

## 🚀 Швидкий старт

1. Виконайте наступну команду для створення віртуального оточення:
    ```
    python3 -m venv .venv 
    ```

2. Для активації віртуального оточення, використовуйте одну з наступних команд, в залежності від вашої системи та командного інтерпретатора:
    - На Windows у командному рядку (CMD):
        ```
        .\.venv\Scripts\activate.bat
        ```
    - На Windows у PowerShell:
        ```
        .\.venv\Scripts\Activate.ps1
        ```
    - На macOS та Linux
        ```
        source .venv/bin/activate
        ```

3. Щоб встановити пакети, перелічені у `requirements.txt`, використовуйте команду:
    ```
    pip install -r requirements.txt
    ```

## ▶️ Запуск бота

1. Переконайтеся, що віртуальне середовище активоване та залежності встановлені.
2. Запустіть застосунок:
    ```
    python main.py
    ```
3. У терміналі зʼявиться інтерактивний prompt:
    - автодоповнення працює тільки для першого слова, щоб не заважати введенню аргументів;
    - повідомлення підсвічуються різними кольорами для статусу операцій.

Адресна книга та нотатки автоматично зберігаються до файлу `data/assistant_bot.pkl`. Каталог `data` створюється під час першого запуску.

## 💡 Доступні команди

| Команда            | Опис                                                                 |
|--------------------|----------------------------------------------------------------------|
| `hello`            | Вітає користувача.                                                   |
| `add`              | Додає контакт або новий номер до існуючого контакту.                 |
| `delete`           | Видаляє контакт із адресної книги.                                   |
| `search <text>`    | Шукає контакти за фрагментом імені, телефону або email.              |
| `change-phone`     | Змінює номер телефону контакту.                                      |
| `phones`           | Виводить усі телефони контакту.                                      |
| `add-email`        | Додає email до контакту.                                             |
| `emails`           | Показує email-и контакту.                                            |
| `change-email`     | Замінює email на новий.                                              |
| `set-address`      | Додає або оновлює адресу контакту.                                   |
| `add-birthday`     | Зберігає дату народження (формат `DD.MM.YYYY`).                      |
| `show-birthday`    | Показує дату народження контакту.                                    |
| `birthdays <days>` | Відображає контакти, яких треба привітати протягом вказаних днів.    |
| `all`              | Показує всю книгу контактів у таблиці з телефонами, email-ами й адресами. |
| `add-note`         | Додає текстову нотатку.                                              |
| `show-notes`       | Виводить усі нотатки у вигляді пронумерованого списку.               |
| `find-note <text>` | Шукає нотатки за довільним фрагментом тексту (регістр неважливий).   |
| `edit-note`        | Редагує обрану нотатку за порядковим номером                         |
| `delete-note`      | Видаляє обрану нотатку за порядковим номером                         |
| `close` / `exit`   | Завершує роботу бота з автоматичним збереженням даних.               |

## 🧱 Архітектура

- `src/assistant_bot.py` — головний цикл, що координує CLI, обробники та сховище, керуючи життєвим циклом `AddressBook` і `Notes`.
- `src/assistant_bot_cli.py` — клас `AssistantCLI` з автодоповненням команд першим словом, кольоровими повідомленнями (`colorama`) і парсингом введення.
- `src/assistant_bot_handlers.py` — клас `AssistantBotHandlers` з операціями над контактами, днями народження та нотатками, а також форматуванням виводу через `tabulate`.
- `src/address_book.py` — сутності `AddressBook`, `Record` та валідація полів (телефон, email, день народження).
- `src/notes.py` — простий список для роботи з нотатками та збереженням їх порядку.
- `src/assistant_bot_storage.py` — клас `AssistantBotStorage`, який відповідає за читання/запис даних у форматі `pickle`.

Таке розділення спрощує підтримку та тестування: логіку вводу/виводу, бізнес-правила і роботу з файлами можна розвивати незалежно.

## ✔️ Корисні підказки

- Збереження виконується автоматично при виході (`close` або `exit`), додатково нічого робити не потрібно.
- При запуску бот один раз показує головне меню з усіма командами — воно не повторюється після кожної дії.
- Якщо команда введена з помилкою, бот підсвітить повідомлення червоним кольором і не змінюватиме дані.
- Табличний вивід (`tabulate`) зручний для копіювання або швидкого пошуку потрібних даних, а нотатки можна використовувати як швидкий TODO-список.
