# 🤖 goit-pycore-hw-assistant-bot

Асистент для роботи з контактами, який підтримує CRUD-операції, нагадування про дні народження, кольоровий вивід (`colorama`) та автодоповнення команд першим словом (`prompt_toolkit`).

## 🚀 Швидкий старт

1. Виконайте наступну команду для створення віртуального оточення:
    ```
    python3 -m venv .venv 
    ```

2. Для активації віртуального оточення, використовуйте одну з наступних команд, в залежності від вашої системи та командного інтерпретатора:
    - На Windows у командному рядку (CMD):
        ```
        .\.venv\Scripts\activate.bat
        ```
    - На Windows у PowerShell:
        ```
        .\.venv\Scripts\Activate.ps1
        ```
    - На macOS та Linux
        ```
        source .venv/bin/activate
        ```

3. Щоб встановити пакети, перелічені у `requirements.txt`, використовуйте команду:
    ```
    pip install -r requirements.txt
    ```

## ▶️ Запуск бота

1. Переконайтеся, що віртуальне середовище активоване та залежності встановлені.
2. Запустіть застосунок:
    ```
    python main.py
    ```
3. У терміналі зʼявиться інтерактивний prompt:
    - автодоповнення працює тільки для першого слова, щоб не заважати введенню аргументів;
    - повідомлення підсвічуються різними кольорами для статусу операцій.

Адресна книга автоматично зберігається до файлу `data/addressbook.pkl`. Каталог `data` створюється під час першого запуску.

## 💡 Доступні команди

| Команда          | Опис                                                   |
|------------------|--------------------------------------------------------|
| `hello`          | Вітає користувача.                                     |
| `add`            | Додає контакт або новий номер до існуючого контакту.   |
| `change`         | Змінює номер телефону контакту.                        |
| `phone`          | Показує всі телефони контакту у вигляді таблиці (`tabulate`). |
| `all`            | Виводить усю книгу контактів у табличному форматі.     |
| `add-birthday`   | Додає дату народження контакту.                        |
| `show-birthday`  | Показує дату народження контакту.                      |
| `birthdays`      | Відображає контакти, яких треба привітати протягом 7 днів. |
| `close` / `exit` | Завершує роботу бота з автоматичним збереженням даних. |

## 🧱 Архітектура

- `src/assistant_bot.py` — головний цикл, що координує CLI, обробники та сховище.
- `src/assistant_bot_cli.py` — клас `AssistantCLI` з автодоповненням команд першим словом, кольоровими повідомленнями (`colorama`) і парсингом введення.
- `src/assistant_bot_handlers.py` — клас `AssistantBotHandlers` з усіма CRUD-операціями та форматуванням виводу через `tabulate`.
- `src/assistant_bot_storage.py` — клас `AssistantBotStorage`, який відповідає за читання/запис адресної книги.

Таке розділення спрощує підтримку та тестування: логіку вводу/виводу, бізнес-правила і роботу з файлами можна розвивати незалежно.

## ✔️ Корисні підказки

- Збереження виконується автоматично при виході (`close` або `exit`), додатково нічого робити не потрібно.
- Якщо команда введена з помилкою, бот підсвітить повідомлення червоним кольором.
- Табличний вивід (`tabulate`) зручний для копіювання або швидкого пошуку потрібних даних.
